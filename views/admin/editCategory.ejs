<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Category</title>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/admin.css">
</head>

<body>

<div class="d-flex">

  <!-- Sidebar -->
  <%- include('../partials/admin/sidebar', { active: 'categories' }) %>

  <!-- MAIN -->
  <div class="flex-grow-1 main-content p-4">

    <!-- Header -->
    <div class="d-flex align-items-center mb-4">
      <a href="/admin/categories" class="me-2 text-dark">
        <i class="fa-solid fa-arrow-left"></i>
      </a>
      <h5 class="fw-bold m-0">Edit Category</h5>
    </div>

    <!-- CARD -->
    <div class="category-card">

      <h6 class="fw-bold">Category Information</h6>
      <p class="text-muted small mb-4">
        Provide the details for your product category.
      </p>

      <form method="POST" action="/admin/categories/edit/<%= category._id %>" enctype="multipart/form-data">

        <!-- Name -->
        <div class="mb-3">
          <label class="form-label-custom">Category Name</label>
          <input type="text" name="name" class="form-control-custom" value="<%= category.name %>" required>
        </div>

        <!-- Description -->
        <div class="mb-3">
          <label class="form-label-custom">Description</label>
          <textarea name="description" class="form-control-custom" rows="4"><%= category.description %></textarea>
        </div>

        <!-- Upload -->
        <div class="mb-3">
          <label class="form-label-custom">Category Image</label>

          <div class="upload-area" onclick="this.querySelector('input').click()">

            <div class="text-center">
              <i class="fa-solid fa-cloud-arrow-up upload-icon"></i>
              <p class="mb-1">Click to upload or drag and drop</p>
              <small class="text-muted">PNG, JPG (Max. 2MB)</small>
            </div>

            <input type="file" name="image" hidden>

          </div>

          <% if(category.image) { %>
            <div class="mt-2">
              <img src="<%= category.image %>" class="preview-img">
            </div>
          <% } %>
        </div>

        <!-- Offer -->
        <div class="mb-4">
          <label class="form-label-custom">Category Offer</label>
          <input type="number" name="offer" class="form-control-custom" value="<%= category.offer %>">
        </div>

        <!-- Buttons -->
        <div class="d-flex justify-content-end gap-2">
          <a href="/admin/categories" class="btn btn-light">Cancel</a>
          <button class="btn-primary-custom">💾 Save Category</button>
        </div>

      </form>

    </div>

  </div>

</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/js/all.min.js"></script>

</body>
</html>
